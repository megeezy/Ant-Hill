// gRPC management API — Phase 4.
// Exposes: GetStatus, ListThreats, OverrideVerdict, ExportLogs
// Transport: tonic over Unix socket or TLS TCP (configurable).
// Authentication: client certificates (mTLS).
